# GridCast Studio · 用电负荷预测工作台

根据工作区的《用电量预测原型系统需求分析.docx》及导师提出的四模块要求实现的可运行科研原型。包含真实数据处理、模型训练、在线推理、可视化、角色权限与受控自适应重训，并非只有静态界面。

> 当前版本完成原型闭环，不等同于合同全部验收。演示数据是合成数据；真实业务精度需使用你的数据做独立实验。系统不执行电网或设备调度。

## 1. 本机启动

这台电脑的 `E:\project` 已安装 Python 虚拟环境、CPU PyTorch 和前端依赖，前端已构建。

双击 **`start.cmd`**，或在 PowerShell 运行：

```powershell
cd E:\project
.\start.cmd
```

默认浏览器地址：`http://127.0.0.1:8000`。首次使用自行创建管理员，没有硬编码默认密码。随后点击总览的“初始化演示场景”，等待训练成功，即可看到曲线、区间、解释和测试集指标。关闭服务用终端 `Ctrl+C`，不要重复启动占用同一端口。

### 新电脑安装

建议 Python **3.12**，Node.js **22 或 24**。不要使用这台电脑系统 PATH 中的 Python 3.7。

```powershell
.\start.cmd -Install -Hybrid
```

`-Install` 创建虚拟环境、安装 Python 依赖并构建前端；`-Hybrid` 安装 CPU 版 PyTorch；只用集成树和基线可不加 `-Hybrid`。首次安装需要网络。依赖和前端构建完成后，本地运行无需联网。

跨平台手动安装：

```bash
python3.12 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
cd frontend
npm ci
npm run build
cd ..
python run.py
```

Windows 手动方式使用 `.venv\Scripts\python.exe`。修改界面后执行 `start.cmd -Rebuild`；若独立开发，先启动后端，再在 `frontend` 执行 `npm run dev`，Vite 会代理 `/api` 到本机 8000 端口。

## 2. 推荐演示顺序

1. **总览**：初始化 90 天小时级合成数据，真实训练并发布演示集成模型，生成一次回测和一次未来 24 步预测。此按钮是特例，会发布新建演示数据集的模型；正常训练不自动上线。
2. **预测分析**：选择未来预测 / 最近留出窗口回测，切换 80%、90%、95% 名义区间；查看历史、点预测、区间、实际值、输入敏感度和逐步区间宽度。未来实际值未到达时，误差显示待回流。
3. **训练与模型**：同一数据版本和预测步长训练季节基线、集成模型、SSM 混合模型。查看实际训练损失、测试指标，管理员可上线或重新发布旧版本回滚。
4. **评估对比**：只在相同数据版本、步长和测试时间窗内比较 MAE、RMSE、WAPE、覆盖率、区间宽度、区间评分和 CRPS。
5. **数据管理**：查看质量和历史曲线，上传自己的 CSV；在数据详情追加新的连续观测。演示数据可点击“追加演示数据”增加 24 小时，随后历史预测匹配到实际值。
6. **运行监测**：打开自动预测或受控重训，保存门槛。新观测达到样本数、冷却期，且误差或偏移超阈值，才生成候选模型；管理员审核再发布。不会因候选效果变差而自动替换线上模型。
7. **时序分解 / 系统设置**：查看 STL 历史分解、创建算法工程师或运营账号、修改自己的密码。

导出支持带溯源字段的 CSV；“打印报告”使用浏览器打印，可选择“另存为 PDF”，不是服务器直接生成 PDF 文件。

## 3. 数据约定

| 列 | 含义 | 要求 |
|---|---|---|
| `timestamp` | 采样时刻 | 必填，推荐 `2026-01-01 00:00:00` |
| `load` | 用电功率，kW | 必填，有限非负数，非累计电表读数 |
| `temperature` | 温度，℃ | 可选 |
| `humidity` | 湿度，% | 可选 |
| `price` | 电价 | 可选，保持统一计价单位 |
| `is_holiday` | 节假日标记 | 可选，建议 0 / 1 |

- CSV 支持 UTF-8 / GB18030，单文件最大 20 MB，单数据集最大 100,000 行。
- 固定采样间隔 5 分钟至 1 天，必须整除 1,440 分钟。缺失时点、缺失负荷、负值不静默补造；重复时点保留最后一条并提示。统计异常保留供核查。
- 无时区时间按北京时间解释；带一致时区的数据转成 Asia/Shanghai。不能混合带时区和不带时区。
- 可选字段在构造特征时前向填充，起始缺失用仅从训练段计算的中位数。未来实测气象不会泄露进预测。
- 追加必须从当前末尾的下一采样时刻开始、至少 2 条，不覆盖历史。每次生成新的不可变 CSV 版本，历史模型保留原训练快照。
- 预测步数不是小时数：15 分钟粒度的 96 步才是 24 小时。目前支持 2–168 步，因此 7 天预测仅在小时级或更粗粒度满足。模型训练后窗口固定，推理沿用该窗口。
- 数据过少会明确失败。建议至少 `max(20 × 预测步数, 4 × 历史窗口长度)` 行；小时级 24 步实验建议至少 30–90 天。两行模板只演示格式，不足以训练。

## 4. 模型与评估边界

### 三类可运行模型

- **季节朴素基线**：重复最近一个日周期，供对照。
- **集成树 + 残差校准**：64 棵 ExtraTrees 对多步季节残差建模，使用历史负荷、日历和可选历史外部变量；独立校准段修正区间尺度。
- **SSM + 稀疏上下文注意力**：真实 PyTorch 选择性状态递推、稀疏上下文注意力、门控融合、残差、归一化和 Dropout；AdamW 训练并保留验证最优权重，推理执行 24 次 MC Dropout 估计离散程度。

**混合模型是研究参考实现，不是官方 Mamba，也没有官方 selective-scan CUDA 内核；不得据此声称已复现论文性能或达到合同的线性复杂度/城市级指标。**当前计算配置为 CPU，官方模型和已有权重需另行适配。

按原始时间序列 60% / 15% / 12.5% / 12.5% 划分训练、验证、校准、测试；后两段的目标窗口彼此不重叠。预处理统计量来自训练段，测试标签不用于拟合或区间校准。长数据最多抽取 2,500 个训练窗口以限制原型计算量。

区间是**独立校准残差得到的经验预测区间**，不是参数置信区间。在时序相关和分布漂移下不承诺分布无关覆盖保证。名义 95% 不等于实际覆盖 95%，必须同时检查实测覆盖率与区间宽度。

CRPS 是使用 48 次校准残差抽样的近似值。解释采用分组输入扰动 / 通道遮蔽，不是 SHAP，不是因果解释，贡献不要求加和等于预测值。STL 用于事后探索，不作为未来已知特征。

“高精度”是需要实验验证的目标，不是软件硬编码承诺。实际精度取决于场景、数据质量、时间跨度和模型配置。系统保存真实指标，不显示伪造的预测准确率。

## 5. 双模式部署

完整服务器部署步骤（Docker Compose 与裸机 systemd + Nginx 两种方式、HTTPS、备份恢复、故障排查、上线检查清单）见 **`docs/部署手册.md`**。交付他人时先运行 `python scripts/make_release.py` 生成含预构建前端的干净 zip。

### 本地

默认仅绑定 `127.0.0.1:8000`，SQLite、CSV 和模型保存在 `.data`。模型训练在独立工作进程，API 保持单进程。复制 `.env.example` 为 `.env` 可改端口、模式和数据目录。

### 云端单实例容器

仓库提供 `Dockerfile`、`compose.yaml` 和持久化命名卷。本次电脑未安装 Docker，所以已提供配置，但**尚未实际构建和验证容器**。

```powershell
Copy-Item .env.example .env
```

在 `.env` 设置随机 `APP_SETUP_TOKEN`（至少 16 位，不使用示例密码），然后：

```bash
docker compose up --build -d
docker compose logs -f gridcast
```

默认包含 CPU PyTorch；仅需轻量模型时设置 `INSTALL_HYBRID=false` 后重新构建。容器以非 root 用户运行，应用端口仅映射宿主机回环。通过 SSH 端口转发访问，或由宿主机的 HTTPS 反向代理转发到 `127.0.0.1:8000`。

公网部署务必：
- 配置有效 HTTPS，并设 `APP_COOKIE_SECURE=true`。
- 配置 `APP_PUBLIC_ORIGIN=https://你的实际域名`，初始化页面填服务器配置的令牌。
- 限制暴露端口，设置访问控制、磁盘配额、上传请求大小上限、日志轮转和外部监控。
- 保留命名卷，不执行会删除数据卷的操作。设置备份并验证恢复。
- **仅运行一个 API 实例 / 一个 worker**。不支持 `uvicorn --workers 4` 或多个容器共享同一存储；启动恢复逻辑和队列针对单实例设计。

两种模式运行同一套程序，不提供本地与云端的自动数据同步。云端仍为 SQLite 单实例原型，不是高可用集群；PostgreSQL、Redis、分布式队列尚未实现。

## 6. 权限、存储与恢复

管理员拥有全部功能；工程师可导入/追加数据、训练和预测；运营账号可查看、预测和导出。首个管理员经事务创建，远程/云端初始化要求令牌。密码 BCrypt，JWT 保存为 HttpOnly / SameSite Cookie，有效 8 小时；签名密钥首次生成于 `.data/.jwt-secret`。

`.data` 包括 `gridcast.db`、版本化 CSV、模型文件、签名密钥和工作日志，均不应公开或提交版本库。模型文件由内部训练生成，通过 joblib 加载；**不要放入来源不可信的模型文件**。

备份：停止应用，整体复制 `.data`，再启动。数据库中的数据集和模型文件路径按**相对路径**存储，`.data` 整体拷贝到其他机器、盘符或 Linux 容器后可直接使用；旧版本数据库中的绝对路径会在服务启动时自动迁移为相对路径。

应用重启后，排队任务保留，原运行中任务标记失败并可重新提交；已保存模型、预测和用户仍在。取消仅限排队任务。暂不提供强制中断计算、密码找回、MFA 或其他会话自动撤销。

## 7. 工程结构

```text
backend/main.py       HTTP API、鉴权入口、静态资源
backend/datasets.py   CSV 校验、版本与增量数据
backend/engine.py     特征、训练、校准、评估与预测
backend/hybrid.py     PyTorch 混合参考模型
backend/service.py    发布、监测与自适应策略
backend/worker.py     独立异步任务工作进程
backend/store.py      SQLite 与持久化
backend/security.py   用户权限与会话
frontend/src/pages/   总览、数据、模型、预测、对比、分解、监测、设置
deploy/               systemd 服务与 Nginx HTTPS 反代示例
scripts/              交付打包脚本 make_release.py
docs/                 需求覆盖、部署手册与验收记录
```

实际接口以服务 `/docs` 的 OpenAPI 为准，采用 HTTP 状态码及 JSON 对象；没有照搬原文 `{code,message,data}` 包装或全部路径。前端采用哈希路由、Vue 3 + ECharts 和自定义绿色主题，而非 Element Plus 蓝色主题。所有字体使用系统字体，资源构建到本地，不依赖 CDN。

## 8. 后续研究与正式交付

优先接入真实业务数据，确定精度、延迟、区间覆盖率验收口径；再接入导师已有的训练完成模型及权重。实际数据流连接器、任意历史预测起点、已知未来气象/节假日计划、行业与空间关联特征、CEEMDAN、分页与缓存、正式 PDF、GPU 和生产级运维是后续阶段。专利、论文、两城市实际部署和经济效益不能由这个软件原型代替验证。

详细需求映射见 `docs/需求覆盖与阶段规划.md`，已执行测试见 `docs/验收记录.md`。
