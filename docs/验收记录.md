# GridCast Studio 原型验收记录

验收日期：2026-09-16。验收对象：当前工作区的本地科研原型。

## 1. 结论与证据边界

四模块已经形成可运行闭环：数据导入与版本管理 → 真实模型训练 → 版本发布与预测 → 点预测、经验区间、解释与实际值回流 → 受控候选重训。

本记录是开发环境的功能与冒烟验证，不是导师或项目方的最终业务验收，不证明真实场景的精度、生产安全等级、并发能力或合同全部指标。

- 正式本机入口：`http://127.0.0.1:8000`，使用默认 `.data`，交付检查时未创建管理员、未注入测试数据。
- 测试入口：`http://127.0.0.1:8001`，使用独立 `.qa/smoke-data`，账号和数据均只用于测试。
- 云端模式使用独立 `.qa/cloud-check` 通过应用测试客户端验证；没有部署到外部服务器，也没有运行 Docker。
- 以下预测指标全部来自合成负荷，不可作为真实电力业务效果证据。

## 2. 验证环境

| 项目 | 实际环境 |
|---|---|
| 操作系统 | Windows，本机 CPU 运行 |
| Python | 项目虚拟环境 3.12.14 |
| Node.js | 24.14.0 |
| 前端构建 | Vite 7.3.6，Vue 3、ECharts |
| 后端 | FastAPI 0.141.1、Uvicorn 0.53.0 |
| 数值与模型 | NumPy 2.5.3、pandas 3.0.5、scikit-learn 1.9.1、statsmodels 0.15.0 |
| 深度学习 | PyTorch 2.8.0+cpu |
| 浏览器验证 | Playwright Chromium 无头浏览器 |
| 页面尺寸 | 桌面 1440 × 1000；手机 390 × 844 |

## 3. 已执行检查

| 检查项 | 实际操作与结果 |
|---|---|
| 前端生产构建 | `npm run build` 成功，生成 `frontend/dist`；最终构建包含登录页内联错误提示 |
| 后端静态检查 | `.venv\Scripts\python.exe -m compileall -q backend run.py` 通过 |
| 依赖一致性 | `.venv\Scripts\python.exe -m pip check` 返回 `No broken requirements found.` |
| 本机启动 | 实际运行 `start.ps1`，8000 健康接口正常、首次初始化页面可见 |
| 初始化与登录 | 重复初始化返回 409；未认证保护接口返回 401；错误密码在登录表单显示错误，正确登录成功 |
| 权限与跨站写入 | 运营角色可读总览但发布模型、访问用户管理返回 403；不受信任 Origin 写入返回 403 |
| 数据校验 | 负负荷、时间缺口、规范化后重复列名、小数秒时间戳被拒绝；5 分钟采样的历史窗口与基线形状检查通过 |
| CSV 界面闭环 | 浏览器上传 72 条小时级 CSV，查看前 12 条原始行，追加 24 条后变为 v2 / 96 条，回流任务成功；未引用数据集可删除 |
| 数据详情与时序分解 | 历史曲线、质量提醒、缺失率、CSV 追加入口可见；STL 接口执行真实分解并返回结果 |
| 三类训练引擎 | 集成树、季节朴素基线及 PyTorch 混合模型均实际训练成功，模型产物和测试指标落盘；混合模型验收仅运行 2 个 epoch |
| 浏览器训练流程 | 从训练表单提交季节基线，任务完成通知正常，模型详情可打开和关闭 |
| 发布与回滚 | 发布基线模型后重新发布归档集成模型，上线状态按预期切换 |
| 未来预测与回测 | 已执行真实未来预测和留出窗口回测；未来标签尚未到达时实际误差为空，回测返回实际值 |
| 经验预测区间 | 同一模型和输入分别计算 80%、90%、95% 区间，边界非负、有序并包含点预测；更高名义水平对应更宽区间 |
| 浏览器预测流程 | 在预测分析切换 90% 区间并提交任务，结果及“名义水平 90%，不等于实测覆盖率”说明更新 |
| 真实值回流 | 追加 24 个演示观测后，原未来预测匹配到 24 个实际值，产生回流 WAPE；CSV 导出含实际值及溯源信息 |
| 受控自适应 | 临时启用低门槛策略，追加数据触发自动预测及候选训练；候选做同窗评估，原上线版本未被自动替换；测试后策略恢复默认关闭 |
| 策略保存 | 浏览器保存运行监测策略，接口及成功提示正常 |
| 全页面与移动端 | 总览、数据、训练、预测、对比、分解、监测、设置均能渲染；390 像素宽度无页面级横向溢出，导航选择后收起 |
| 浏览器错误 | 最终关键流程与移动端遍历未捕获 JavaScript `pageerror`；使用美国洛杉矶浏览器时区完成流程，数据展示保持系统设定时区 |
| 持久化与工作进程 | 在无运行中任务时停止并重新启动测试服务，1 个数据集、5 个模型、9 条预测记录的 ID 集合摘要完全一致；原账号仍能登录，独立工作进程重新在线 |
| 云端应用模式 | 独立测试实例中，缺少初始化令牌返回 403，正确令牌初始化返回 200；静态首页、登录退出和角色流程通过 |

### 重启验证范围

本次检查的是已完成任务和记录的持久化、登录密钥持久化以及计算进程重建。没有进行断电、磁盘故障、运行中训练恢复、跨实例竞争或灾难恢复演练。实现中的运行中断任务会标记失败，而不是自动续训。

## 4. 合成数据结果摘录

初始合成数据为 90 天小时级负荷、数据版本 v1、24 步预测。以下指标取自相同版本的独立测试窗口，默认名义区间为 95%。

| 模型 | 测试 MAE / kW | 实测区间覆盖率 |
|---|---:|---:|
| 季节朴素基线 | 51.3552 | 95.4545% |
| 集成树 + 残差校准 | 23.5260 | 96.9697% |
| 选择性状态递推 + 稀疏上下文注意力混合模型，2 epoch | 42.0902 | 96.5909% |

- 混合模型仅验证训练推理链路，不代表调优结果，也不是官方 Mamba 复现。
- 对同一预测输入测试的 80% / 90% / 95% 区间平均宽度约为 90.29 / 132.86 / 164.12 kW；这里只证明该用例的区间顺序和数值有效性。
- 增量自适应用例中，旧模型与候选在同一更新测试窗的 MAE 分别约为 23.9230 / 23.4503 kW。候选保持待发布，旧模型保持上线。
- 后续浏览器新训练的 v2 季节基线 MAE 为 47.4173 kW，不能与上表 v1 直接排名。
- 名义区间水平不等于实测覆盖率；上述结果不提供任意未来时间序列上的覆盖保证。

## 5. 可复核材料与演示步骤

页面截图保存在 `.qa`：

- `dashboard-final.png`：最终总览，包含真实模型输出、区间、敏感度和版本状态。
- `predict-desktop.png`、`predict-mobile.png`：预测结果的桌面与手机布局。
- `data-detail.png`：数据详情和追加入口。
- `first-run.png`：首次管理员初始化页面。

这些截图来自测试实例，不表示正式入口已经有业务数据。`.qa` 是本地测试输出，不需要作为运行依赖分发。

建议使用自己的新账号在正式入口复核：

1. 创建管理员，初始化演示场景，等待训练、回测和未来预测任务完成。
2. 查看预测曲线、区间、敏感度和留出评估；确认未来误差在标签到达前不显示虚构数值。
3. 训练同版本的基线或混合模型，查看任务日志和模型对比，手动发布或回滚。
4. 追加演示观测，检查历史预测实际值、回流误差和监测事件。
5. 按需要启用自适应门槛，确认候选需要人工审核上线。
6. 按模板导入自己的 CSV，使用真实场景重新训练和评估。

详细配置和安全部署说明见根目录 `README.md`；逐项需求边界见 `docs/需求覆盖与阶段规划.md`。

## 6. 未验证或尚未实现

- 未使用用户真实业务数据验证“高精度”，没有承诺固定 MAE、MAPE 或统一准确率。
- 未执行 Docker 构建、云服务器部署、HTTPS 反向代理验收或多机同步；当前云端方案是单实例持久化部署配置。
- 未进行生产并发、最大数据量、长期稳定性、外网渗透或量化低于 1 秒的性能验收。
- 未接电表、MQTT、Kafka 或外部实时采集服务；“实时”指追加接口接入、后台处理与每 4 秒界面刷新。
- 自适应是监测驱动的批量重训，不是逐条在线参数更新；候选发布由管理员控制。
- 解释采用分组扰动 / 通道遮蔽，不是 SHAP，也不证明因果关系。
- 时序分解为 STL，未实现 CEEMDAN；PDF 为浏览器打印，非服务器直接生成。
- 未验证正式 Mamba 模型、GPU 训练、多城市部署、论文、专利或经济效益。

建议下一阶段先用真实数据和导师正式模型完成同窗实验，再确定指标阈值、采集接入和生产部署范围。

## 4. 交付加固记录（2026-09-18）

针对"提交他人 / 部署服务器"的交付目标所做的加固与验证，均在本机完成：

- **存储路径可移植化**：`datasets.path` 与 `models.artifact` 改为相对 `.data` 的相对路径存储；新增 `store.data_path()` 读取兼容（旧库绝对路径自动重定位到当前数据目录下的同名文件）与 `store.migrate_paths()` 启动迁移（旧库绝对路径自动改写为相对路径，幂等）。已用临时数据目录实测：旧格式库迁移、未迁移旧库兼容读取、新写入相对路径、`save_new`/`load_frame` 回读全部通过。
- **部署配套**：新增 `deploy/gridcast.service`（systemd）与 `deploy/nginx-gridcast.conf`（HTTPS 反代示例）；新增 `docs/部署手册.md`（Docker Compose 与裸机两种路径、初始化、备份恢复迁移、故障排查、上线检查清单）。
- **交付打包**：新增 `scripts/make_release.py`，从 git 干净工作区导出源码，附加预构建 `frontend/dist` 与需求 docx，生成带 SHA256 的 zip。本次已实际生成并验证包结构。
- **本机冒烟**：`compileall` 通过；独立数据目录 + 8001 端口启动，`/api/health` 正常，初始化/登录/演示数据/训练/预测/导出 CSV 闭环复验通过。
- 本机仍无 Docker，容器路径仍未实际构建验证；裸机 systemd/Nginx 配置为按标准写法提供的示例，未在真实 Linux 服务器上执行过。

## 5. 测试套件与代码规范（2026-09-18 二次补充）

- 新增 `tests/` pytest 套件共 13 个用例：CSV 校验规则 8 个（缺列、负负荷、时间缺口、小数秒、混合时区、重复时点、一致时区转换、质量摘要）、路径可移植性 3 个（相对写入、旧库迁移、兼容读取）、业务闭环集成 2 个（云端令牌初始化→演示训练→发布→预测→导出；重启后登录与回测）。`pytest -m "not integration"` 可跳过含真实训练的集成用例。
- 新增 `pyproject.toml`（ruff 规则：豁免 FastAPI B008 惯用法、worker 裸异常按设计豁免并注明原因；`requirements-dev.txt` 提供开发依赖）。修复全部 lint 问题：未使用 import、`zip` 缺 `strict`、异常链 `from`、`subprocess` 缺显式 `check`、多余 `set()` 等。
- 测试过程发现并修复一个真实缺陷：pandas 3 对混合时区输入直接抛英文原生错误，绕过了原有中文提示分支；现已在解析入口统一转换为中文错误信息。
- 本机最终验证：`pytest` 13/13 通过，`ruff check` 全绿，`compileall` 通过。
